namespace GraphTask
{
    public class GraphTaskMain
    {
        static void Main()
        {
            //Graph graph = new Graph(7, 0, 1, 2, 6, 5, 1, 4, 5, 1, 3);

            //Action<int> action = (x) => { Console.Write(x); Console.Write(", "); };

            //graph.BypassByWidth(action);
            //Console.WriteLine();
            //graph.BypassByDeep(action);
            //Console.WriteLine();
            //graph.BypassByRecursiveDeep(action);
        }
    }
}